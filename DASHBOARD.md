# 운영 대시보드

로그인 후 `http://localhost:5175/dashboard` 또는 상단 **대시보드** 메뉴에서 확인합니다. 실행 중인 백엔드는 재시작하여 새 API를 반영합니다.

## 집계 기준

`GET /api/dashboard`는 기존 JWT 인증을 사용하며 한 응답으로 아래 데이터를 반환합니다.

| 항목 | 기준 |
| --- | --- |
| 전체 현장 수 | 모든 Site 상태 합계 |
| 전체 장비 수 | 모든 Equipment 상태 합계 |
| 정상 장비 수 | Equipment의 NORMAL |
| 장애 장비 수 | Equipment의 ERROR |
| 미처리 유지보수 | Maintenance의 REPORTED + IN_PROGRESS |
| 관제 이벤트 수 | 모든 VideoEvent 상태 합계 |
| 상태별 현황 | 현장·장비·유지보수·관제 이벤트 각각의 상태별 건수 |
| 최근 유지보수 장애 / 관제 이벤트 | occurredAt 내림차순, 같은 시간이면 id 내림차순, 각각 최대 5건 |

전체 기간에 대한 현재 DB 집계이며, 관제 이벤트에는 차량번호 인식 데이터가 아닌 기존 영상분석 이벤트를 사용합니다. 완료된 장애와 이벤트도 최근 발생 목록에 포함됩니다. 유지보수의 현장은 연결된 장비의 현재 소속 현장입니다.

DB에서 4개의 상태별 GROUP BY 쿼리와 2개의 제한된 최근 목록 쿼리를 실행합니다. 대시보드 응답은 읽기 전용 트랜잭션의 동일한 DB 스냅샷을 사용합니다. 기존 Entity, 기능별 Repository/Service, Security 및 DB 설정은 변경하지 않습니다.

## 화면 확인

- 6개 KPI와 상태 분포 도넛·막대 그래프에 실제 건수·비율이 표시되는지 확인합니다.
- 최근 장애의 현장·장비·상태, 최근 이벤트의 위치·유형·위험도를 확인합니다.
- KPI 및 관리/전체 보기 링크로 기존 관리 화면에 이동할 수 있습니다.
- 다른 관리 화면에서 데이터를 변경한 뒤 돌아와 **새로고침**하면 최신 집계를 가져옵니다.
- 데이터가 없으면 0건과 빈 목록을 표시하며, 조회 실패 시 오류 메시지를 표시합니다. 재조회가 실패하면 이전 데이터를 유지하고 오래된 집계임을 알립니다.
- 창 너비를 줄이면 카드가 재배치됩니다. 차트는 CSS 기반이며 새 라이브러리는 사용하지 않습니다.

## 검증

- Backend: `cd backend` 후 `./gradlew.bat build`
- Frontend: `cd frontend` 후 `npm run build`
- `DashboardServiceTest`: 빈 DB의 0건 처리, 모든 상태 포함, 미처리 계산, 큰 건수 처리.
- `DashboardApiIntegrationTest`: 실제 DB 집계 증가분, JWT 인증, 최근 목록 제한·발생 시간 정렬·현장 연결, 비인증 접근 차단. 테스트 데이터는 종료 시 롤백합니다.
